"""Quiz -> pass/fail prediction package.

Predicts whether a student passes or fails the final exam (binary
classification) from quiz-preparation features. See ``src/train.py`` for the
end-to-end pipeline that trains and logs Logistic Regression, XGBoost and an
MLP to Weights & Biases.
"""
